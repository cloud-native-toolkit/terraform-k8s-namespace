id: github.com/cloud-native-toolkit/terraform-k8s-namespace
name: namespace
description: Creates a namespace in the cluster
platforms:
  - kubernetes
  - ocp3
  - ocp4
tags:
  - infrastructure
  - namespace
versions:
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-iks-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        type: string
        description: The path to the config file for the cluster
        moduleRef:
          id: cluster
          output: config_file_path
      - name: name
        type: string
        description: The namespace that should be created
        scope: module
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
    version: v3.2.3
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-iks-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        type: string
        description: The path to the config file for the cluster
        moduleRef:
          id: cluster
          output: config_file_path
      - name: name
        type: string
        description: The namespace that should be created
        scope: module
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
    version: v3.2.2
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-iks-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        type: string
        description: The path to the config file for the cluster
        moduleRef:
          id: cluster
          output: config_file_path
      - name: name
        type: string
        description: The namespace that should be created
        scope: module
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
    version: v3.2.1
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-iks-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        type: string
        description: The path to the config file for the cluster
        moduleRef:
          id: cluster
          output: config_file_path
      - name: name
        type: string
        description: The namespace that should be created
        scope: module
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
    version: v3.2.0
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-iks-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
        optional: true
    version: v3.1.4
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-iks-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
        optional: true
    version: v3.1.3
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
        optional: true
    version: v3.1.2
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_type
        moduleRef:
          id: cluster
          output: type_code
        type: string
        description: >-
          The type of cluster that should be created (openshift or ocp3 or ocp4
          or kubernetes)
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: tls_secret_name
        moduleRef:
          id: cluster
          output: tls_secret_name
        type: string
        description: The name of the secret containing the tls certificate for the cluster
        default: ''
        optional: true
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
        optional: true
    version: v2.8.0
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
        optional: true
    version: v3.1.1
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
      - name: create_operator_group
        type: bool
        description: >-
          Flag indicating that an operator group should be created in the
          namespace
        default: true
        optional: true
    version: v3.1.0
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
          - source: github.com/ibm-garage-cloud/terraform-ocp-login
            version: '>= 1.0.0'
    variables:
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
    version: v3.0.2
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-ibm-ocp-vpc
            version: '>= 1.0.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
    version: v3.0.1
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_type
        moduleRef:
          id: cluster
          output: type_code
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: tls_secret_name
        moduleRef:
          id: cluster
          output: tls_secret_name
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
    version: v3.0.0
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_type
        moduleRef:
          id: cluster
          output: type_code
        type: string
        description: >-
          The type of cluster that should be created (openshift or ocp3 or ocp4
          or kubernetes)
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: tls_secret_name
        moduleRef:
          id: cluster
          output: tls_secret_name
        type: string
        description: The name of the secret containing the tls certificate for the cluster
        default: ''
        optional: true
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
    version: v2.7.3
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_type
        moduleRef:
          id: cluster
          output: type_code
        type: string
        description: >-
          The type of cluster that should be created (openshift or ocp3 or ocp4
          or kubernetes)
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: tls_secret_name
        moduleRef:
          id: cluster
          output: tls_secret_name
        type: string
        description: The name of the secret containing the tls certificate for the cluster
        default: ''
        optional: true
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
    version: v2.7.2
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_type
        moduleRef:
          id: cluster
          output: type_code
        type: string
        description: >-
          The type of cluster that should be created (openshift or ocp3 or ocp4
          or kubernetes)
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: tls_secret_name
        moduleRef:
          id: cluster
          output: tls_secret_name
        type: string
        description: The name of the secret containing the tls certificate for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
    version: v2.7.1
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_type
        moduleRef:
          id: cluster
          output: type_code
        type: string
        description: >-
          The type of cluster that should be created (openshift or ocp3 or ocp4
          or kubernetes)
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: tls_secret_name
        moduleRef:
          id: cluster
          output: tls_secret_name
        type: string
        description: The name of the secret containing the tls certificate for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
    version: v2.7.0
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_type
        moduleRef:
          id: cluster
          output: type_code
        type: string
        description: >-
          The type of cluster that should be created (openshift or ocp3 or ocp4
          or kubernetes)
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: tls_secret_name
        moduleRef:
          id: cluster
          output: tls_secret_name
        type: string
        description: The name of the secret containing the tls certificate for the cluster
      - name: name
        scope: module
        type: string
        description: The namespace that should be created
    version: v2.6.1
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_type
        moduleRef:
          id: cluster
          output: type_code
        type: string
        description: >-
          The type of cluster that should be created (openshift or ocp3 or ocp4
          or kubernetes)
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: tls_secret_name
        moduleRef:
          id: cluster
          output: tls_secret_name
        type: string
        description: The name of the secret containing the tls certificate for the cluster
        optional: true
      - name: name
        type: string
        description: The namespace that should be created
    version: v2.6.0
    outputs:
      - name: name
        description: Namespace name
  - dependencies:
      - id: cluster
        refs:
          - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
            version: '>= 1.7.0'
          - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
            version: '>= 2.0.0'
    variables:
      - name: cluster_type
        moduleRef:
          id: cluster
          output: type_code
        type: string
        description: >-
          The type of cluster that should be created (openshift or ocp3 or ocp4
          or kubernetes)
      - name: cluster_config_file_path
        moduleRef:
          id: cluster
          output: config_file_path
        type: string
        description: The path to the config file for the cluster
      - name: tls_secret_name
        moduleRef:
          id: cluster
          output: tls_secret_name
        type: string
        description: The name of the secret containing the tls certificate for the cluster
        optional: true
      - name: name
        type: string
        description: The namespace that should be created
    version: v2.5.2
    outputs:
      - name: name
        description: Namespace name
